{
  "name": "faceproof-local-blockchain",
  "version": "1.0.0",
  "private": true,
  "description": "Local Hardhat blockchain verification layer for FaceProof",
  "scripts": {
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "demo": "node demo.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.15"
  },
  "dependencies": {
    "ethers": "^6.13.4"
  }
}
